{
  "name": "trabscript",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "dependencies": {
    "http-server": "^0.12.3"
  },
  "devDependencies": {
    "compile": "tsc",
    "concurrently": "^6.0.2",
    "lite-server": "^2.6.1",
    "server": "lite-server --baseDir=app",
    "watch": "tsc -w"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Anna",
  "license": "ISC"
}
